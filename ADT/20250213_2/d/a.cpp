#include <bits/stdc++.h>
using namespace std;

int main() {
 int n, k, q;
 cin >> n >> k >> q;
 
 vector<int> a(k), l(q);
  for (int i = 0; i < k; i++) {
    cin >> a.at(i);
  }
  for (int i = 0; i < q; i++) {
    cin >> l.at(i);
  }
  

}