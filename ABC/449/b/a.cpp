/*
#include <bits/stdc++.h>
using namespace std;

int main() {
	int h, w, q;
	cin >> h >> w >> q;
	while (q--) {
		int t;
		cin >> t;
		if (t == 1) {
			int r;
			cin >> r;
			cout << r * w << '\n';
			h -= r;
		}
		else {
			int c;
			cin >> c;
			cout << h * c << '\n';
			w -= c;
		}
	}
}
*/